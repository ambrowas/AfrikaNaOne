<resources>
    <!-- App Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryVariant</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <item name="colorSecondary">@color/accentColor</item>
        <item name="colorOnSecondary">@android:color/black</item>

        <item name="android:colorBackground">@color/backgroundColor</item>
        <item name="colorSurface">@color/backgroundColor</item>
        <item name="colorOnSurface">@color/textPrimary</item>

        <!-- Apply Global Button Style -->
        <item name="materialButtonStyle">@style/RoundedButton</item>
    </style>

    <!-- Global Button Style -->
    <style name="RoundedButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_rounded</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:elevation">0dp</item> <!-- Prevents shadow from hiding stroke -->
    </style>
    <style name="CustomAlertDialogTheme" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="android:textColorPrimary">@android:color/white</item> <!-- Dialog Title & Message -->
        <item name="colorAccent">@android:color/white</item> <!-- For Buttons -->
        <item name="android:buttonStyle">@style/CustomDialogButtonStyle</item>
    </style>

    <!-- Custom Style for Dialog Buttons -->
    <style name="CustomDialogButtonStyle" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>